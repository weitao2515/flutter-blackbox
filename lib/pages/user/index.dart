import 'package:flutter/material.dart';

class User extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(
        title: Text('11321321'),
      ),
      body: Scaffold(
        appBar: AppBar(
          title: Text('data'),
        ),
        body: Text('222222222222'),
      ),
    );
  }
}